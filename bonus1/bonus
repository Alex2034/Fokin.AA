'''
название файла ex1 ни о чем не говорит, поэтому нужен док-стринг, из которого будет понятно, о чем прога
дальше я не буду это писать, но это замечание актуально для каждой задачи
'''
p = [2]
c = 0
for n in range(3, 1001):  # bonus.py:6:17: E231 missing whitespace after ','
    for pj in p:  # можно перебирать не все множители, подумай как
        if n % pj == 0:  # bonus.py:8:13: E228 missing whitespace around modulo operator
            c += 1 # можно выходить из цикла как только нашелся 1 множитель. почитай про break
    if c == 0:
        p.append(n)
    c = 0  # можно организовать так, чтобы с=0 было только в 1 месте. меньше повторяющегося кода = лучше.
print(p)

# оценка 2/10
# ты даже не отладил код на автопроверке(
# файлы называются *.py